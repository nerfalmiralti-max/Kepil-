NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Legacy service-role credential for seed and server-side registration. Never prefix this with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
# Preferred server-side registration credential; use a Supabase sb_secret key.
SUPABASE_SECRET_KEY=
# Required explicitly for seed; use a fresh password, at least 12 characters.
DEMO_PASSWORD=
