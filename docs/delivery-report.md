# Проверка поставки KEPIL · 25 сентября 2026

- **Production:** https://kepil.vercel.app, Vercel-проект `kepil` в аккаунте `nerfalmiralti-5552`.
- **GitHub:** https://github.com/nerfalmiralti-max/Kepil-.git, ветка `main`.
- **Supabase:** проект `peujijzrosdmirotpnhh` в организации `nerfalmiralti-max's Org`, регион `ap-south-1`. Две миграции применены к живой БД. Созданы 5 Auth-пользователей и профилей, 4 организации, 3 подрядчика, 3 договора, 14 объектов и 13 гарантий. Сквозные и браузерные проверки создали реальные заявки и историю в PostgreSQL.
- **Auth и роли:** вход ADMIN, INSPECTOR, CONTRACTOR A и CONTRACTOR B выполнен. Подрядчик B получает экран «Запись не найдена» для заявки A и перенаправляется с реестра. Прямые RPC и RLS отвергли доступ к чужой заявке, редактирование гарантии и подтверждение собственного ремонта.
- **Storage:** браузер загрузил реальный PNG в приватный bucket `claim-evidence`; после перезагрузки инспектор увидел фото, подписанный URL вернул HTTP 200 и `image/png`. Прямое чтение объекта без подписи отвергнуто. `npm run test:live` проверил ограничения другого подрядчика на получение ссылки.
- **Браузерный цикл:** инспектор открыл действующую гарантию объекта `AKT-W-001`, создал дефект трубы и заявку `80f9af09-5c8a-40c8-9147-6616fbeec2b9`; подрядчик принял заявку, начал ремонт, приложил фото и отправил результат; инспектор проверил фото и подтвердил ремонт. После перезагрузки статус остался `VERIFIED`, хронология и аудит содержат события. Второй дефект создал заявку `ff6d1429-336c-4acb-a7d8-053b252a8326` и получил отметку повторного случая. Панель отразила сохранённые данные.
- **Отрицательные проверки:** истёкшая гарантия и объект без гарантии не создают заявку; короткое описание отвергнуто; неверный переход и подтверждение подрядчиком отвергнуты; прямое изменение гарантии, чужая заявка, анонимное чтение, закрытый маршрут и прямое чтение файла заблокированы.
- **Окружение:** production содержит только `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. Локальный пароль демо-аккаунтов хранится в игнорируемом `.local/demo-credentials.md`. Service-role key не используется приложением. Вход в production нормализует URL Supabase до origin, поскольку рантайм Vercel добавлял `/rest/v1/`; исправление находится в `src/lib/supabase/server.ts` и `src/proxy.ts`.
- **Границы проверки:** браузерный цикл выполнен в Chromium на 1440 px. Панель, объект и заявка проверены на 390 px без горизонтального переполнения. Проверка 1024 px, клавиатуры и сценарий отклонения ремонта в production не выполнялись. Подключения к реальным муниципальным реестрам нет; все записи демонстрационные.

**Финальные проверки:** `npm.cmd run lint` — 0 ошибок; `npm.cmd run typecheck` — успешно; `npm.cmd test` — 24/24 успешно; `npm.cmd run build` — успешно, все маршруты Next.js 16.3.6 собраны. Production deployment `dpl_5pMP1dkB2G9jD2xCrcdwWUJWqgiS` готов и доступен через публичный адрес.
